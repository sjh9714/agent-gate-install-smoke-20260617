# Composite PR proof

The declared task for this pull request is to add this documentation page. The
same pull request intentionally includes a workflow permission escalation and
an agent instruction change so Agent Gate can demonstrate deterministic
change-control evidence on a real public GitHub Actions run.
