# Agent Gate Install Smoke

This public sandbox repository verifies a first-time external install of
`sjh9714/Agent-Gate@v0.1.3`.

The test PR is intentionally left open and unmerged.

PR #68 smoke: default config fallback when agent-gate.yml is absent.
